[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: requires running docker services"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]